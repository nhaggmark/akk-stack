fastapi==0.115.6
uvicorn[standard]==0.34.0
llama-cpp-python==0.3.4
pydantic==2.10.4
chromadb>=0.5.0
sentence-transformers>=3.0.0
# torch is listed so sentence-transformers does not pull a CUDA build.
# The Dockerfile installs CPU-only torch first via --index-url before this
# requirements.txt is processed, so pip treats the requirement as satisfied.
torch>=2.0.0
